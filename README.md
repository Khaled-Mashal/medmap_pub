# 🏥 MedMap - Medical Services Management System

نظام إدارة الخدمات الطبية مع Docker

## 📋 المحتويات
- [نظرة عامة](#نظرة-عامة)
- [المتطلبات](#المتطلبات)
- [التثبيت السريع](#التثبيت-السريع)
- [الاستخدام](#الاستخدام)
- [الوثائق](#الوثائق)
- [الدعم](#الدعم)

## 🎯 نظرة عامة

MedMap هو نظام متكامل لإدارة الخدمات الطبية يتضمن:
- ✅ API مبني على ASP.NET Core 8.0
- ✅ قاعدة بيانات PostgreSQL
- ✅ Nginx كـ Reverse Proxy
- ✅ واجهة React مدمجة
- ✅ نظام مصادقة JWT
- ✅ تقارير DevExpress

## 🔧 المتطلبات

### الحد الأدنى
- Docker 20.10+
- Docker Compose 2.0+
- 4 GB RAM
- 20 GB Storage

### الموصى به
- Docker 24.0+
- Docker Compose 2.20+
- 8 GB RAM
- 50 GB SSD Storage

## ⚡ التثبيت السريع

### 1. تثبيت Docker (إذا لم يكن مثبتاً)
```bash
make install-docker
# أو
curl -fsSL https://get.docker.com | sh
```

### 2. إعداد المشروع
```bash
make setup
# أو
cp .env.example .env
nano .env  # عدّل كلمة المرور
```

### 3. تشغيل التطبيق
```bash
make start
# أو
./start.sh
```

### 4. التحقق من التشغيل
```bash
make health
# أو
./health-check.sh
```

## 🚀 الاستخدام

### الأوامر الأساسية

```bash
make start          # تشغيل التطبيق
make stop           # إيقاف التطبيق
make restart        # إعادة التشغيل
make logs           # عرض السجلات
make health         # فحص الصحة
make backup         # نسخ احتياطي
make update         # تحديث التطبيق
make ssl            # إعداد SSL
```

### الوصول إلى التطبيق

- **الإنتاج**: http://medmapgloble.com
- **محلي**: http://localhost
- **API**: http://localhost:5000

## 📚 الوثائق

- [دليل التشغيل الكامل](دليل_التشغيل.md) - دليل شامل بالعربية
- [Docker Documentation](README_DOCKER.md) - وثائق Docker التفصيلية

## 🛠️ الإدارة

### مراقبة الأداء
```bash
make monitor        # مراقبة مستمرة
make stats          # إحصائيات سريعة
make ps             # حالة الحاويات
```

### إدارة قاعدة البيانات
```bash
make db             # قائمة إدارة قاعدة البيانات
make backup         # نسخ احتياطي
make restore        # استعادة
```

### بيئة التطوير
```bash
make dev            # تشغيل بيئة التطوير
make dev-stop       # إيقاف بيئة التطوير
```

## 🔐 الأمان

### قائمة التحقق
- [ ] تغيير `POSTGRES_PASSWORD` في `.env`
- [ ] تغيير `JWT_SECRET_KEY` في `appsettings.json`
- [ ] إعداد SSL/HTTPS
- [ ] إعداد Firewall
- [ ] نسخ احتياطية دورية

### إعداد SSL
```bash
make ssl
# أو
./setup-ssl.sh
```

## 📦 البنية

```
medmap/
├── publish/                 # التطبيق المنشور
│   ├── Dockerfile          # صورة Docker للتطبيق
│   ├── appsettings.json    # إعدادات التطبيق
│   └── wwwroot/            # الملفات الثابتة
├── nginx/                   # إعدادات Nginx
│   ├── nginx.conf          # الإعداد الرئيسي
│   └── conf.d/             # إعدادات المواقع
├── docker-compose.yml       # إعداد الإنتاج
├── docker-compose.dev.yml   # إعداد التطوير
├── Makefile                # أوامر مختصرة
└── *.sh                    # سكريبتات الإدارة
```

## 🔄 التحديثات

```bash
# 1. ضع الملفات الجديدة في مجلد publish
# 2. قم بالتحديث
make update

# أو يدوياً
docker-compose build api
docker-compose up -d api
```

## 💾 النسخ الاحتياطي

### تلقائي
```bash
make backup
```

### يدوي
```bash
# قاعدة البيانات
docker-compose exec postgres pg_dump -U postgres medical_services_db > backup.sql

# الملفات
docker run --rm -v medmap_uploads_data:/data -v $(pwd):/backup alpine tar czf /backup/uploads.tar.gz -C /data .
```

## 🐛 استكشاف الأخطاء

### التطبيق لا يعمل
```bash
make logs           # عرض السجلات
make health         # فحص الصحة
make restart        # إعادة التشغيل
```

### قاعدة البيانات
```bash
make db             # إدارة قاعدة البيانات
docker-compose logs postgres
```

### Nginx
```bash
docker-compose logs nginx
docker-compose exec nginx nginx -t
```

## 📊 المراقبة

### مراقبة مستمرة
```bash
./monitor.sh
```

### فحص سريع
```bash
make health
```

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة
3. Commit التغييرات
4. Push إلى الفرع
5. فتح Pull Request

## 📝 الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE)

## 📞 الدعم

- **الوثائق**: [دليل_التشغيل.md](دليل_التشغيل.md)
- **المشاكل**: افتح Issue على GitHub
- **البريد الإلكتروني**: support@medmapgloble.com

## 🎯 الميزات القادمة

- [ ] دعم Kubernetes
- [ ] مراقبة متقدمة (Prometheus/Grafana)
- [ ] CI/CD Pipeline
- [ ] نسخ احتياطي تلقائي إلى السحابة
- [ ] دعم متعدد اللغات

## 🙏 شكر خاص

- ASP.NET Core Team
- PostgreSQL Community
- Nginx Team
- Docker Community

---

**تم التطوير بواسطة فريق MedMap** 💙

للمزيد من المعلومات، راجع [الوثائق الكاملة](دليل_التشغيل.md)

