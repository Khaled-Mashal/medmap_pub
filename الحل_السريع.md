# 🚨 حل سريع للمشاكل الشائعة

## ❌ خطأ: Permission denied

### المشكلة
```
./update.sh: line 45: ./backup.sh: Permission denied
```

### الحل السريع ⚡
```bash
make fix-permissions
```

أو

```bash
chmod +x *.sh
```

---

## 🔄 تحديث الواجهة الأمامية

### الخطوات الكاملة

#### 1️⃣ إصلاح الصلاحيات أولاً
```bash
make fix-permissions
```

#### 2️⃣ تحديث الفرونت إند
```bash
make update-frontend
```

أو للتحديث الكامل:
```bash
make update
# اختر الخيار 3 (كلاهما)
```

#### 3️⃣ اختر الخيار الصحيح
عندما يسألك السكريبت:
```
الخيارات المتاحة:
  1. استخدام النطاق: https://medmapgloble.com
  2. استخدام عنوان نسبي: /api (موصى به)  ⭐
  3. إدخال عنوان مخصص
```

**اختر الخيار 2** (عنوان نسبي) - هذا هو الأفضل!

#### 4️⃣ امسح كاش المتصفح
```
Ctrl + Shift + Delete
```
ثم أعد تحميل الصفحة:
```
Ctrl + F5
```

---

## 📋 قائمة التحقق السريعة

### قبل التحديث
- [ ] تأكد من وجود ملفات الفرونت إند في `publish/wwwroot/build`
- [ ] قم بإصلاح الصلاحيات: `make fix-permissions`
- [ ] تأكد من أن Docker يعمل: `docker ps`

### أثناء التحديث
- [ ] اختر الخيار 2 (عنوان نسبي)
- [ ] انتظر حتى يكتمل التحديث
- [ ] تحقق من عدم وجود أخطاء

### بعد التحديث
- [ ] امسح كاش المتصفح
- [ ] أعد تحميل الصفحة (Ctrl+F5)
- [ ] افتح أدوات المطور (F12)
- [ ] تحقق من عناوين الطلبات في تبويب Network

---

## 🔍 التحقق من نجاح التحديث

### 1. تحقق من الملفات
```bash
docker run --rm -v medmap_frontend_data:/data alpine ls -la /data
```

### 2. تحقق من عدم وجود localhost
```bash
docker run --rm -v medmap_frontend_data:/data alpine cat /data/static/js/main.*.js | grep "localhost:5000"
```

إذا لم يظهر شيء = نجح التحديث ✅

### 3. تحقق من المتصفح
1. افتح F12 (أدوات المطور)
2. اذهب إلى Network
3. أعد تحميل الصفحة
4. تحقق من عناوين الطلبات

يجب أن تكون:
```
✅ /api/auth/login
✅ /api/users
```

وليس:
```
❌ http://localhost:5000/api/auth/login
```

---

## 🛠️ حل المشاكل الشائعة

### المشكلة 1: لا يزال يطلب localhost
**الحل:**
```bash
# 1. امسح كاش المتصفح تماماً
# 2. جرب في وضع التصفح الخاص
# 3. أعد تشغيل Nginx
docker-compose restart nginx
```

### المشكلة 2: خطأ CORS
**الحل:**
تحقق من `publish/appsettings.json`:
```json
"AllowedOrigins": [
  "http://localhost:3000",
  "http://localhost",
  "https://medmapgloble.com"
]
```

### المشكلة 3: 404 Not Found
**الحل:**
```bash
# تحقق من Nginx
docker-compose logs nginx

# أعد تشغيل Nginx
docker-compose restart nginx
```

### المشكلة 4: الملفات لم تتحدث
**الحل:**
```bash
# احذف volume وأعد إنشاءه
docker-compose down
docker volume rm medmap_frontend_data
make update-frontend
```

---

## 🚀 الأوامر المفيدة

### إصلاح الصلاحيات
```bash
make fix-permissions
```

### تحديث الفرونت إند
```bash
make update-frontend
```

### تحديث كامل
```bash
make update
```

### عرض السجلات
```bash
make logs
```

### فحص الصحة
```bash
make health
```

### إعادة التشغيل
```bash
make restart
```

---

## 📞 إذا استمرت المشكلة

### 1. عرض السجلات
```bash
docker-compose logs nginx
docker-compose logs api
```

### 2. فحص الحاويات
```bash
docker-compose ps
```

### 3. التحقق من الاتصال
```bash
curl -I http://localhost
curl -I http://localhost:5000
```

### 4. إعادة البناء الكاملة
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

## 💡 نصيحة للمستقبل

### في كود React
استخدم دائماً عناوين نسبية:

```javascript
// ❌ خطأ
const API_URL = "http://localhost:5000";

// ✅ صحيح
const API_URL = "";

// الاستخدام
axios.get(`${API_URL}/api/users`);
// النتيجة: /api/users
```

### ملف .env في React
```env
REACT_APP_API_URL=
```

### في الكود
```javascript
const API_URL = process.env.REACT_APP_API_URL || "";
```

---

## ✅ الخلاصة

1. **أصلح الصلاحيات**: `make fix-permissions`
2. **حدّث الفرونت إند**: `make update-frontend`
3. **اختر الخيار 2** (عنوان نسبي)
4. **امسح الكاش**: Ctrl+Shift+Delete
5. **أعد التحميل**: Ctrl+F5
6. **تحقق**: F12 > Network

**جاهز! 🎉**

